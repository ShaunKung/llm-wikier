# AGENTS.md - LLM Wikier 配置文件

此文件定义知识库的结构、约定和工作流程。

> ⚠️ **重要提示**：本文档除「用户偏好」和「自定义配置」章节外，其余章节均由工具包在更新安装时从模板自动刷新。请勿在其他章节添加个人内容，否则更新安装时将被覆盖。您的自定义内容请仅存放在「用户偏好」和「自定义配置」章节中。

## 知识库概述

这是一个由 LLM Wikier 管理的个人知识库。LLM 会增量构建和维护一个结构化的 markdown wiki，所有 cross-references 已经建立，矛盾已经标注，综合反映所有已读取的内容。

## 三层架构

1. **Raw Sources**：知识库中的原始文件，不可变
2. **Wiki**：LLM 生成的 markdown 文件集合，存放在 `.wiki/` 目录
3. **Schema**：本配置文件，定义结构、约定和工作流程

## Wiki 目录结构

```
.wiki/
├── index.md          # 内容索引（按类别组织）
├── log.md            # 操作日志（时间顺序）
├── entities/         # 实体页面（人物、组织、项目等）
├── concepts/         # 概念页面（主题、理论、方法等）
├── sources/          # 源文件摘要
└── analysis/         # 分析与综合页面
```

## 页面命名规范

- **使用小写字母和连字符**：`my-topic.md`，不要用空格或下划线
- **中文可用**：`实体名称.md` 或 `话题主题.md`
- **实体页面**：`entities/实体名称.md`
- **概念页面**：`concepts/概念名称.md`
- **源文件摘要**：`sources/源文件名-summary.md`
- **分析页面**：`analysis/分析主题.md`

## 支持的文件格式

**文本格式**：
- 文档：`.md`, `.txt`, `.rst`, `.org`, `.tex`
- 数据：`.json`, `.yaml`, `.yml`, `.csv`, `.xml`
- 网页：`.html`
- 代码：常见编程语言的源代码文件

**办公文档格式**：
- PDF：`.pdf`
- Word：`.docx`, `.doc`
- PowerPoint：`.pptx`, `.ppt`
- Excel：`.xlsx`, `.xls`
- OpenDocument：`.odt`, `.odp`, `.ods` (可选)

**图片格式**：`.png`, `.jpg`, `.jpeg`, `.gif`, `.webp`, `.svg`, `.bmp`

## 视觉内容处理策略

主 agent 可能是纯文本模型，不具备图像/视觉能力。为此，知识库支持通过 `vision-reader` subagent 处理视觉内容。

### 触发条件
以下文件类型需要视觉能力处理：
- 纯图片文件：`.png`, `.jpg`, `.jpeg`, `.gif`, `.webp`, `.svg`, `.bmp`
- 办公文档的视觉元素：`.pptx`, `.ppt`, `.pdf`, `.docx`, `.doc`
- 网页和 Markdown 中的嵌入图片

### 处理流程

**纯图片文件**（始终委托）：
1. 使用 Task 工具调用 `vision-reader` subagent 读取
2. 基于返回的文字描述进行知识提取

**办公文档 & 网页**（两段式）：
1. 使用 Read 工具直接读取，获取文本内容
2. 使用 Task 工具调用 `vision-reader` subagent 读取视觉元素
3. 合并两段结果进行知识提取

**Markdown**（文本优先）：
1. 使用 Read 工具读取文本内容
2. 仅当图片引用对理解内容关键时，按需调用 `vision-reader`

### vision-reader 配置

如未配置 `vision-reader`（即 `.opencode/agents/vision-reader.md` 不存在），Agent 应跳过视觉处理，仅处理文本内容。

配置方式：使用 LLM Wikier 提供的 `config_vision_reader` 脚本，或在 `.opencode/agents/` 下手动创建 `vision-reader.md`。

## 文件排除规则

知识库根目录的 `.wiki_ignore` 文件定义了被排除的文件和目录。
格式类似 `.gitignore`：每行一个模式，`#` 开头的行为注释。

### 默认排除项
- `.opencode/` — skills 配置目录
- `.wiki/` — wiki 内容本身
- `.git/` — 版本控制
- `AGENTS.md` — 知识库配置文件
- `output/` — 用户自产文件（展示文档、报告等），不会被作为源文件处理

### 用户自定义
用户可在 `.wiki_ignore` 的「用户自定义规则」区域添加自己的排除项，
例如添加 `*.pptx` 排除所有 PowerPoint 文件，或添加 `temp/` 排除临时目录。

### 对技能的影响
所有扫描 raw sources 的技能（wiki-init、wiki-ingest、wiki-update）在扫描文件前
必须读取 `.wiki_ignore` 并按其中规则排除匹配的文件和目录。

## 主动知识抓取

Agent 应在所有对话中主动感知用户提供的新知识，无需等待用户发出指令。

### 行为规则

- **自动感知**：对话中出现以下信号时，Agent 应自动检测并提议沉淀：
  - 用户陈述了 wiki 中未记录的事实、数据
  - 用户纠正了此前的说法或 wiki 中的记录
  - 用户表达了新的观点、见解或偏好
  - 用户做了决策或选择
- **默认提议，不自动写入**：检测到新知识后先向用户提议，获得确认后再写入
- **冲突必问**：如新知识与 wiki 已有内容冲突，必须暂停并请用户裁决
- **不重复捕获**：同一会话中已提议过的内容不再重复提议
- **安静失败**：如用户忽略或拒绝，不反复追问

### 冲突处理规则

当新信息与 wiki 已有记录冲突时：

1. 明确报告冲突详情（新信息 vs wiki 记录 vs wiki 中记录的原始来源）
2. 请用户裁决：
   - **旧记录有误** → 更正 wiki，在"争议历史"章节记录变更
   - **新信息有误** → 不修改 wiki，记录到日志
   - **两者共存** → 添加"不同来源记载"章节，分别列出
3. 不要自行判断谁对谁错

### 工作流

具体的抓取、比对、冲突处理和写入流程详见 `wiki-capture` skill。

## 核心操作

### 1. Ingest（入库）

添加新源文件时：
1. 读取源文件内容
2. 提取关键实体、概念、观点
3. 创建或更新相关 wiki 页面
4. 建立页面间链接
5. 更新 `index.md` 和 `log.md`
6. 在 `.wiki/.wiki-processed` 记录处理状态

### 2. Query（查询）

基于 wiki 回答问题时：
1. 先阅读 `.wiki/index.md` 找到相关页面
2. 阅读相关页面内容
3. 综合信息回答问题
4. **重要答案可沉淀为新页面** 存入 `.wiki/analysis/`

### 3. Lint（健康检查）

定期检查 wiki 健康状态：
- 矛盾：不同页面的信息冲突
- 孤立页面：没有入链的页面
- 缺失页面：被引用但未创建的页面
- 过时信息：需要更新的内容

### 4. Update（更新）

重新处理已处理的源文件：
- 当源文件内容更新时
- 当需要重新提取信息时

### 5. Prune（清理）

清理 wiki 中的无效内容：
- 删除孤立页面
- 清理失效链接
- 合并重复内容

### 6. Capture（抓取）

从对话中自动捕获新知识：
- Agent 自动感知对话中的新事实、纠正、观点、决定
- 默认提议，用户确认后写入 wiki
- 如与已有内容冲突，先请用户裁决再修正
- 冲突处理后保留"争议历史"记录

## 文件追踪机制

`.wiki/.wiki-processed` JSON 格式：

```json
{
  "version": 1,
  "entries": [
    {
      "path": "articles/foo.md",
      "hash": "sha256:abc123...",
      "processed": "2026-05-03T10:00:00Z"
    }
  ]
}
```

## 视觉内容处理

对于需要视觉能力的文件类型（图片、办公文档、网页），参见上文「视觉内容处理策略」章节。

## 日志格式

`.wiki/log.md` 条目格式：

```markdown
## [YYYY-MM-DD HH:MM] 操作类型 | 对象

### 处理内容
- [具体操作列表]

### 结果
- [处理结果摘要]

---
```

操作类型包括：`ingest`, `query`, `lint`, `update`, `prune`

## 索引格式

`.wiki/index.md` 按类别组织：

```markdown
# Wiki 索引

## 实体页面
- [[entities/实体A|实体A]] - 一句话描述
- [[entities/实体B|实体B]] - 一句话描述

## 概念页面
- [[concepts/概念A|概念A]] - 一句话描述

## 源文件摘要
- [[sources/文件名-summary|文件名]] - 一句话描述

## 分析页面
- [[analysis/主题|主题]] - 一句话描述

---
*最后更新: YYYY-MM-DD*
*总页面数: N*
```

## 冲突处理

当新信息与已有信息矛盾时：
1. 在相关页面添加 `## 争议` 或 `## 不同观点` 章节
2. 分别列出不同来源的观点
3. 标注每个观点的来源

## 最佳实践

- **及时入库**：添加新文件后尽快运行 `/wiki-ingest`
- **定期检查**：每周运行 `/wiki-lint` 保持 wiki 健康
- **沉淀答案**：重要的查询答案可以作为分析页面保存
- **保持索引**：确保 `index.md` 反映最新内容
- **标注来源**：每个信息点都应追溯到源文件

## 自动备份

每次新会话开始时，Agent 应以自动模式静默执行一次备份：

    bash .opencode/skills/wiki-backup/backup.sh --auto

- 不向用户显示执行过程和结果
- 同一会话内仅执行一次（自动模式的日级去重机制已覆盖此要求）
- 如脚本不存在，静默跳过

## 用户偏好

<!-- 用户可以在此添加个人偏好 -->

## 自定义配置

<!-- 用户可以在此添加自定义配置 -->
