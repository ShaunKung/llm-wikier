# AGENTS.md - LLM Wikier 配置文件

此文件定义知识库的结构、约定和工作流程。

## 知识库概述

这是一个由 LLM Wikier 管理的个人知识库。LLM 会增量构建和维护一个结构化的 markdown wiki，所有 cross-references 已经建立，矛盾已经标注，综合反映所有已读取的内容。

## 三层架构

1. **Raw Sources**：知识库中的原始文件，不可变
2. **Wiki**：LLM 生成的 markdown 文件集合，存放在 `wiki/` 目录
3. **Schema**：本配置文件，定义结构、约定和工作流程

## Wiki 目录结构

```
wiki/
├── index.md          # 内容索引（按类别组织）
├── log.md            # 操作日志（时间顺序）
├── entities/         # 实体页面（人物、组织、项目等）
├── concepts/         # 概念页面（主题、理论、方法等）
├── sources/          # 源文件摘要
└── analysis/         # 分析与综合页面
```

## 页面命名规范

- **使用小写字母和连字符**：`my-topic.md`，不要用空格或下划线
- **中文可用**：`实体名称.md` 或 `话题主题.md`
- **实体页面**：`entities/实体名称.md`
- **概念页面**：`concepts/概念名称.md`
- **源文件摘要**：`sources/源文件名-summary.md`
- **分析页面**：`analysis/分析主题.md`

## 支持的文件格式

**文本格式**：
- 文档：`.md`, `.txt`, `.rst`, `.org`, `.tex`
- 数据：`.json`, `.yaml`, `.yml`, `.csv`, `.xml`
- 网页：`.html`
- 代码：常见编程语言的源代码文件

**办公文档格式**：
- PDF：`.pdf`
- Word：`.docx`, `.doc`
- PowerPoint：`.pptx`, `.ppt`
- Excel：`.xlsx`, `.xls`
- OpenDocument：`.odt`, `.odp`, `.ods` (可选)

**图片格式**：`.png`, `.jpg`, `.jpeg`, `.gif`, `.webp`, `.svg`, `.bmp`

## 办公文档处理策略

对于 PDF、Word、PowerPoint、Excel 等办公文档：
1. LLM 直接读取文档内容（支持原生格式）
2. 提取文本、表格、图片等信息
3. 对于包含图片的文档，可单独查看图片获取额外上下文
4. 处理结果记录在源文件摘要中

注意：办公文档的处理结果可能与原文排版有差异，关键信息会保留。

## 排除目录

以下目录不会被处理：
- `wiki/`（wiki 内容本身）
- `.opencode/`（skills 配置）
- `.git/`（版本控制）

## 核心操作

### 1. Ingest（入库）

添加新源文件时：
1. 读取源文件内容
2. 提取关键实体、概念、观点
3. 创建或更新相关 wiki 页面
4. 建立页面间链接
5. 更新 `index.md` 和 `log.md`
6. 在 `.wiki-processed` 记录处理状态

### 2. Query（查询）

基于 wiki 回答问题时：
1. 先阅读 `wiki/index.md` 找到相关页面
2. 阅读相关页面内容
3. 综合信息回答问题
4. **重要答案可沉淀为新页面** 存入 `wiki/analysis/`

### 3. Lint（健康检查）

定期检查 wiki 健康状态：
- 矛盾：不同页面的信息冲突
- 孤立页面：没有入链的页面
- 缺失页面：被引用但未创建的页面
- 过时信息：需要更新的内容

### 4. Update（更新）

重新处理已处理的源文件：
- 当源文件内容更新时
- 当需要重新提取信息时

### 5. Prune（清理）

清理 wiki 中的无效内容：
- 删除孤立页面
- 清理失效链接
- 合并重复内容

## 文件追踪机制

`.wiki-processed` JSON 格式：

```json
{
  "version": 1,
  "entries": [
    {
      "path": "articles/foo.md",
      "hash": "sha256:abc123...",
      "processed": "2026-05-03T10:00:00Z"
    }
  ]
}
```

## 图片处理策略

对于包含图片的 markdown 文件：
1. 优先处理文本内容
2. 在需要时单独查看相关图片获取额外上下文
3. 图片分析结果记录在源文件摘要中

## 日志格式

`wiki/log.md` 条目格式：

```markdown
## [YYYY-MM-DD HH:MM] 操作类型 | 对象

### 处理内容
- [具体操作列表]

### 结果
- [处理结果摘要]

---
```

操作类型包括：`ingest`, `query`, `lint`, `update`, `prune`

## 索引格式

`wiki/index.md` 按类别组织：

```markdown
# Wiki 索引

## 实体页面
- [[entities/实体A|实体A]] - 一句话描述
- [[entities/实体B|实体B]] - 一句话描述

## 概念页面
- [[concepts/概念A|概念A]] - 一句话描述

## 源文件摘要
- [[sources/文件名-summary|文件名]] - 一句话描述

## 分析页面
- [[analysis/主题|主题]] - 一句话描述

---
*最后更新: YYYY-MM-DD*
*总页面数: N*
```

## 冲突处理

当新信息与已有信息矛盾时：
1. 在相关页面添加 `## 争议` 或 `## 不同观点` 章节
2. 分别列出不同来源的观点
3. 标注每个观点的来源

## 最佳实践

- **及时入库**：添加新文件后尽快运行 `/wiki-ingest`
- **定期检查**：每周运行 `/wiki-lint` 保持 wiki 健康
- **沉淀答案**：重要的查询答案可以作为分析页面保存
- **保持索引**：确保 `index.md` 反映最新内容
- **标注来源**：每个信息点都应追溯到源文件

## 用户偏好

<!-- 用户可以在此添加个人偏好 -->

## 自定义配置

<!-- 用户可以在此添加自定义配置 -->
